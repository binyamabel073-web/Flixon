name: Build Flixon APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Grab the files from your repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Extract the project files handling spaces and parentheses safely
      - name: Unzip Flixon Project
        run: |
          if [ -f "flixon (1).zip" ]; then
            unzip "flixon (1).zip" -d flixon
          elif [ -f "Flixon (1).zip" ]; then
            unzip "Flixon (1).zip" -d flixon
          elif [ -f "flixon/flixon (1).zip" ]; then
            unzip "flixon/flixon (1).zip" -d flixon
          elif [ -f "Flixon/Flixon (1).zip" ]; then
            unzip "Flixon/Flixon (1).zip" -d flixon
          elif [ -f "flixon.zip" ]; then
            unzip flixon.zip -d flixon
          else
            echo "Error: Could not find flixon (1).zip or flixon.zip"
            exit 1
          fi

      # Step 3: Set up Java environment
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # Step 4: Load Gradle execution systems
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # Step 5: Construct the missing Gradle wrapper setup
      - name: Generate missing Gradle Wrapper
        run: |
          cd flixon
          if [ ! -f "build.gradle" ] && [ ! -f "build.gradle.kts" ]; then
            SUBDIR=$(find . -maxdepth 2 -name "build.gradle" -o -name "build.gradle.kts" | head -n 1 | cut -d'/' -f2)
            if [ ! -z "$SUBDIR" ] && [ "$SUBDIR" != "." ]; then cd $SUBDIR; fi
          fi
          gradle wrapper
          chmod +x gradlew

      # Step 6: Generate the mandatory test keystore for signing
      - name: Generate Debug Keystore
        run: |
          cd flixon
          if [ ! -f "build.gradle" ] && [ ! -f "build.gradle.kts" ]; then
            SUBDIR=$(find . -maxdepth 2 -name "build.gradle" -o -name "build.gradle.kts" | head -n 1 | cut -d'/' -f2)
            if [ ! -z "$SUBDIR" ] && [ "$SUBDIR" != "." ]; then cd $SUBDIR; fi
          fi
          keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "C=US, O=Android, CN=Android Debug"

      # Step 7: Compile and assemble the app binary
      - name: Build with Gradle
        run: |
          cd flixon
          if [ ! -f "build.gradle" ] && [ ! -f "build.gradle.kts" ]; then
            SUBDIR=$(find . -maxdepth 2 -name "build.gradle" -o -name "build.gradle.kts" | head -n 1 | cut -d'/' -f2)
            if [ ! -z "$SUBDIR" ] && [ "$SUBDIR" != "." ]; then cd $SUBDIR; fi
          fi
          ./gradlew assembleDebug --stacktrace

      # Step 8: Expose the resulting installation file
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: flixon-apk
          path: flixon/**/build/outputs/apk/debug/*.apk
